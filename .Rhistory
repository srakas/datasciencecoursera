completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
result <- dapply(total, .(ID), function(y) cor(y$sulfate y$nitrate)) #cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
result <- dapply(total, .(ID), function(y) cor(y$sulfate y$nitrate)) #cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr <- function(directory, threshold = 0) {
reqire(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
result <- dapply(total, .(ID), function(y) cor(y$sulfate y$nitrate)) #cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr <- function(directory, threshold = 0) {
reqire(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate y$nitrate)) #cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr <- function(directory, threshold = 0) {
reqire(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
reqire(plyr)
if (nrow(total) > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
# result <- daply(total, .(ID), function(y) cor(y$sulfate y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
result <- daply(total, .(ID), function(y) cor(y))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr <- function(directory, threshold = 0) {
require(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate, y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
head(result)
}
corr("specdata", 150)
install.packages("plyr")+
install.packages("plyr")
devtools::install_github("hadley/plyr")
corr("specdata", 150)
corr <- function(directory, threshold = 0) {
require(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(total) > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate, y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
result
}
cr <- corr("specdata", 150)
head(cr)
summary(cr)
cr < corr("specdata", 400)
head(cr)
summary(cr)
cr <- corr("specdata", 5000)
corr <- function(directory, threshold = 0) {
require(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (exists(total) && nrow(total) > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate, y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
result
}
cr <- corr("specdata", 5000)
corr <- function(directory, threshold = 0) {
require(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
totalRows = 0
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
totalRows += completeCasesNumber
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(totalRows) > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate, y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
result
}
corr <- function(directory, threshold = 0) {
require(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
totalRows = 0
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
totalRows += completeCasesNumber
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
}
corr <- function(directory, threshold = 0) {
require(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
totalRows = 0
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
totalRows =  totalRows + completeCasesNumber
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (nrow(totalRows) > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate, y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
result
}
cr <- corr("specdata", 5000)
corr <- function(directory, threshold = 0) {
require(plyr)
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
totalRows = 0
firstFile <- TRUE
for (i in 1:332) {
filepath = file.path(directory, paste(sprintf("%03d", i), '.csv', sep=""))
filedata = read.csv(filepath)
completeCasesFile <- na.omit(filedata)
completeCasesNumber <- nrow(completeCasesFile)
if (completeCasesNumber > threshold) {
totalRows =  totalRows + completeCasesNumber
if (firstFile) {
total <- completeCasesFile
firstFile <- FALSE
}
else {
total <- rbind(total, completeCasesFile)
}
}
}
if (totalRows > 0) {
result <- daply(total, .(ID), function(y) cor(y$sulfate, y$nitrate))
#cor(total[, 2:3])
}
else {
result <- numeric()
}
result
}
cr <- corr("specdata", 5000)
summary(cr)
length(cr)
cr <- corr("specdata")
length(cr)
summary(cr)
submit()
submit()
submit()
